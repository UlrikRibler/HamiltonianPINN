# NeuroManifold Configuration 🌌
# PRESET: GOLD STANDARD PRECISION
# Optimized for Maximum Statistical Fidelity (High ESS, Zero Energy Drift)

defaults:
  - _self_

seed: 42

model:
  hidden_dim: 20
  num_layers: 3
  input_dim: 2
  output_dim: 1

hmc:
  # Sampling Density
  num_samples: 200
  burn_in: 100
  
  # Integrator Precision ("Small Steps, Long Paths")
  step_size: 5e-4  # Small enough to keep Acceptance ~100% (High Numerical Precision)
  num_steps: 100   # Long enough to decorrelate samples (High Statistical Precision)
  
  # Geometry Adaptation
  adapt_mass_matrix: true
  beta: 100.0

train:
  pretrain_epochs: 500
  lr: 1e-3

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}